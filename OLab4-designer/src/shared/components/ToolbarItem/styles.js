import { DARK_BLUE } from '../../colors';

const styles = {
  iconButton: {
    fontSize: 0,
    padding: 8,
    margin: '0 8px',
  },
  iconButtonActive: {
    border: `1px dashed ${DARK_BLUE}`,
    padding: 6,
  },
};

export default styles;
