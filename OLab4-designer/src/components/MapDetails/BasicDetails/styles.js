import styled from 'styled-components';

export const ContainerKeywords = styled.div`
  margin: 15px 0;
`;

const styles = () => ({
  textField: {
    marginLeft: 5,
    width: '100%',
  },
});

export default styles;
